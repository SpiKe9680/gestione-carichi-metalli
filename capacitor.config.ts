import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.metalflow.app',
  appName: 'Movimentazione Metalli',
  webDir: 'build'
};

export default config;
